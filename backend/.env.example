# Backend environment (local or Render)
JWT_SECRET=change-me-to-a-long-random-string
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# Optional Postgres (if empty, server runs in-memory)
DATABASE_URL=
# CORS (set to your frontend URL in production; "*" to start)
CORS_ORIGIN=*
